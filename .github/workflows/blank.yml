name: Manual Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set variable
        run: echo "MY_VAR=HelloVinod" >> $GITHUB_ENV

      - name: Use variable
        run: echo "Value is $MY_VAR"
