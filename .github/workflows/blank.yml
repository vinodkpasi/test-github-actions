name: My Project
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: exit 0

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Lint
        run: exit 0

  test:
    runs-on: ubuntu-latest
    needs: [build, lint]
    steps:
      - name: Test
        run: exit 1
