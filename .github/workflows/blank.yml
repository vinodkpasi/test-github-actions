name: Manual Workflow

on:
  workflow_dispatch:

jobs:
  build:
    steps:
      - name: Install
        run: exit 0
   lint:
    steps:
      - name: Lint
        run: exit 0
   test:
    steps:
      - name: Test
        run: exit 1

